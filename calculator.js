// retorna o valor da soma entre os dois parâmetros
function sum(v1, v2) {
  return v1 + v2;
}
console.log(sum(10, 10));

// retorna o valor da subtração entre os dois parâmetros
function sub(v1, v2) {// Importa a função de subtração do arquivo subtracao.js

  // Exemplo de uso da função de subtração
  return v1 - v2
     
  ;}

  console.log(sub(10, 5))
// retorna o valor da divisão entre os dois parâmetros
function div(v1, v2) {}

// retorna o valor da multiplicação entre os dois parâmetros

function mult(v1, v2) {
    const Resultado = v1 * v2;
    console.log(`${Resultado}`);
    return Resultado
}


// retorna o valor da raiz quadrada do valor recebido por parâmetro
function square(v1) {
  return Math.sqrt(v1);
}
console.log(square(25));
// Exemplo de uso da função da raiz
/*
const raiz = Math.sqrt(25);
console.log(raiz); // Output: 5
*/
